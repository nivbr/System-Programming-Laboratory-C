generally:
	_handle errors.
	_use exit(1) on failed amalloc()
main:

preprocessor:

passeges:

pass1:
	_calculate L for normal ops.
	_sure that data without LABLE before also procseed DC ?
	_define the writing to codeMem
	_make sure comment lines treated well
	_make sure at least one of {IC,DC} procseed at each iteration (which isn't comment\empty)
	
pass2:

symbolChart:
	_make sure resizing works good.