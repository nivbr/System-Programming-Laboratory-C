generally:
	_handle errors.
	_use exit(1) on failed amalloc()

passeges:
	_fix comment line (they crash the program)(empty ones re OK).

symbolChart:
	_make sure resizing works good.


*valgrind command:
	valgrind --leak-check=yes ./assembler exampletest